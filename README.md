# Testing vite react app using msw + jest/vitest

Example repository on how to test vite react app using jest or vitest and msw.

Running tests using vitest: `npm run vitest`

Running tests using jest: `npm run jest`
